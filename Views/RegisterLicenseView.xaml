<Window x:Class="LicenseManagement.EndUser.Wpf.Views.RegisterLicenseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LicenseManagement.EndUser.Wpf.Views"
        mc:Ignorable="d"
        xmlns:rules='clr-namespace:LicenseManagement.EndUser.Wpf.Rules'
        Name='thisView'
        Title="Register License"
        FontFamily="Segoe UI"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="White"
        Width="460">
  <Window.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source='pack://application:,,,/LicenseManagement.EndUser.Wpf;component/AppResources.xaml' />
      </ResourceDictionary.MergedDictionaries>
      <rules:ReceiptCodeRule x:Key='ReceiptCodeRule' />
    </ResourceDictionary>
  </Window.Resources>

  <StackPanel Margin='26,24,26,22'>
    <!-- header -->
    <StackPanel Orientation='Horizontal'>
      <Border Width='38' Height='38' CornerRadius='10' VerticalAlignment='Center'>
        <Border.Background>
          <LinearGradientBrush StartPoint='0,0' EndPoint='1,1'>
            <GradientStop Color='#1F2A37' Offset='0' />
            <GradientStop Color='#36506B' Offset='1' />
          </LinearGradientBrush>
        </Border.Background>
        <TextBlock Text='&#x26BF;' Foreground='White' FontSize='18' HorizontalAlignment='Center' VerticalAlignment='Center' />
      </Border>
      <StackPanel Margin='13,0,0,0' VerticalAlignment='Center'>
        <TextBlock Text='Register your license' Style='{StaticResource LmHeading}' FontSize='18' />
        <TextBlock Text='Enter the product key from your purchase receipt.' Style='{StaticResource LmCaption}' Margin='0,2,0,0' />
      </StackPanel>
    </StackPanel>

    <!-- product key -->
    <TextBlock Text='Product key' Style='{StaticResource LmCaption}' FontWeight='SemiBold'
               Foreground='{StaticResource LmInkBrush}' Margin='0,20,0,6' />
    <TextBox x:Name='receiptCode'
             Style='{StaticResource LmTextBox}'
             Validation.ErrorTemplate='{StaticResource noStylesTemplate}'>
      <TextBox.Text>
        <Binding Path='ReceiptCode' UpdateSourceTrigger='PropertyChanged'>
          <Binding.ValidationRules>
            <rules:ReceiptCodeRule />
          </Binding.ValidationRules>
        </Binding>
      </TextBox.Text>
    </TextBox>
    <!-- validation message -->
    <TextBlock Margin='2,7,0,0' MinHeight='18' FontSize='12' FontFamily='Segoe UI'
               Foreground='{StaticResource LmDangerBrush}' TextWrapping='Wrap'
               Text='{Binding ElementName=receiptCode, Path=(Validation.Errors)/ErrorContent}' />

    <!-- buttons -->
    <StackPanel Orientation='Horizontal' HorizontalAlignment='Right' Margin='0,18,0,0'>
      <Button Content='Cancel' IsCancel='True' Style='{StaticResource LmGhostButton}' MinWidth='92' />
      <Button x:Name='Submit'
              Content='Register'
              Command='{Binding Register}'
              CommandParameter='{x:Reference Name=thisView}'
              Margin='10,0,0,0' MinWidth='112'>
        <Button.Style>
          <Style TargetType='Button' BasedOn='{StaticResource LmPrimaryButton}'>
            <Style.Triggers>
              <DataTrigger Binding='{Binding ElementName=receiptCode, Path=(Validation.HasError)}' Value='True'>
                <Setter Property='IsEnabled' Value='False' />
              </DataTrigger>
            </Style.Triggers>
          </Style>
        </Button.Style>
      </Button>
    </StackPanel>
  </StackPanel>
</Window>
